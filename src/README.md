# src/

Application / library source code lives here.

Keep architecture notes, source maps, and key implementation details in
`docs/technical-context.md`, and the runbook in `docs/environment.md`. Per the
documentation contract in `AGENTS.md`, any meaningful change here must update at
least one relevant document, and must be proven with real evidence (see the
verification workflow).
